# CIIP Battery Intelligence Framework — Python Dependencies
# Core dependencies for scoring engine, UBO classifier, and data analysis

# Data analysis
pandas>=2.0.0
numpy>=1.24.0

# Visualization (for Streamlit dashboard)
plotly>=5.14.0
matplotlib>=3.7.0

# Statistical analysis (for Stage 2 PCA upgrade)
scikit-learn>=1.3.0
scipy>=1.11.0

# Network analysis (for Stage 3 v5.0 Network Centrality Model)
networkx>=3.1

# Report generation
reportlab>=4.0.0

# Presentation generation
pptxgenjs  # via Node.js (see package.json)

# Data collection (Stage 2)
requests>=2.31.0
# UN Comtrade API — no additional package needed, uses requests

# Web application (Streamlit dashboard)
streamlit>=1.25.0

# Utilities
python-dotenv>=1.0.0
